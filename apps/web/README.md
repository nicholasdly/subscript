# web

A playground for [`@nicholasdly/subscript`](../../packages/subscript). Type a query — or pick an example — and see the JSON result.

```sh
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
